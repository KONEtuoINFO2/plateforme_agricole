[build]
  base = "backend"
  publish = "public"
  command = "composer install --no-interaction --prefer-dist --optimize-autoloader && npm install && npm run build || true"

[build.environment]
  NODE_VERSION = "18"
  PHP_VERSION = "8.2"